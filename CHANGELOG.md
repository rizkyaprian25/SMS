# CHANGELOG

Format: `YYYY-MM-DD — ringkasan`.

## 2026-09-10
- Inisialisasi monorepo SMS (apps/services/packages/infra/docs).
- 12 dokumen desain lengkap di `docs/` (00 overview, 01 arsitektur multi-device pemula, 02 database, 03 backend, 04 web, 05 mobile guru, 06 fase 2, 07 kontrak API, 08 auth+RBAC, 09 face recognition, 10 notifikasi+offline sync, 11 workflow+roadmap).
- `AGENTS.md` + `README.md` + `infra/docker/docker-compose.yml` (postgres + minio).
- Skema penamaan: kebab-case (`apps/mobile-guru`, `services/api-server`). Scope MVP dikunci: mobile guru + web dashboard + satu backend.
- Scaffold runnable: `services/api-server` (NestJS + `prisma/schema.prisma` 17 model + `seed.ts` 22 rombel + `openapi.yaml`, `prisma validate` ✅, `nest build` ✅), `apps/web-dashboard` (Next.js 15 + login/ringkasan/siswa/rombel, `next build` ✅ 7 halaman), `apps/mobile-guru` (Flutter + Riverpod/go_router/Dio + 4 screen inti, `flutter analyze` ✅ no issues), `packages/shared-types` (enum + DTO, `tsc` ✅).
- Skill + standar scalable: 4 skill (`.opencode/skills/sms-{backend,data,web,flutter}-model`), `docs/12-standar-kode.md`, modul contoh `src/rombel/` (service+spec, `npm test` 3/3 ✅, `eslint` bersih), contoh `use-rombel.ts` + `query-keys.ts` (web), `page.dart` + `rombel_repository.dart` (mobile), `opencode.json` (skill allow).
- Modul domain inti: `siswa` (scope role + NISN mask, 4 test), `jadwal` (deteksi bentrok guru/rombel + 4 test), `absensi` (bulk idempoten + rekap + edit hari-H + audit, 7 test), `nilai` (bulk + cek pengampu, 2 test), helper `common/access.ts`, `openapi.yaml` dilengkapi path baru.
- Presensi + izin + dashboard: `absensi-guru` (face score/liveness/geofence di server, fallback PENDING, verifikasi admin, rekap keterlambatan vs `SEKOLAH_JAM_MASUK`, 6 test), `perizinan` (ajukan maks 30 hari, putuskan wali-binaan-only + auto absensi IZIN/SAKIT per jadwal + audit, 4 test), `dashboard` (ringkasan 5 angka, 1 test). Total 9 suites / 31 test ✅.
- Auth sesi + BK + DB nyata: refresh rotation (anti replay, cabut per device/semua) + `PUT /auth/devices/token`, model `sesi`, modul `pengumuman` + `pelanggaran` (scope BK/wali + total poin), migrasi `init_sms_core` + seed terverifikasi di postgres (22 rombel/11 mapel/1 admin), smoke test HTTP (login + ringkasan + rombel paginasi ✅). Postgres host dipindah ke 5433 (bentrok proyek lain).
- Rapor + export: `rapor` (rekap rata-rata per mapel + capaian + PDF via pdf-lib, 3 test), `reports` (`absensi.xlsx` + `nilai.xlsx` via exceljs, maks 5000 baris, 2 test). Total 13 suites / 48 test ✅.
- Master web-ready: `guru` (CRUD + akun login 1 transaksi + atur mapel + arsip, 3 test), `mapel` (list/create, 2 test), `POST /siswa/import` (xlsx maks 5MB, buat/update/lapor per baris, 3 test); halaman web `/siswa` (+import), `/guru` (+mapel, RHF+zod), `/jadwal` (tampil 409 bentrok ramah).
- Web tuntas 14 rute (`next build` ✅): `/absensi-siswa` (filter + rekap server), `/absensi-guru` (rekap keterlambatan + verifikasi PENDING), `/nilai`, `/perizinan` (setujui/tolak), `/pengumuman` (terbit/hapus + target role), `/pelanggaran` (catat + total poin), `/laporan` (unduh xlsx + rapor PDF per siswa), nav global di layout, komponen `selects` + `query-keys` terpusat.
- Mobile inti nyata: `GET /rombel/:id/siswa` (+status hari itu, 2 test baru → 60 ✅), session (secure storage + cookie refresh + interceptor 401→refresh→retry + daftar FCM), login/jadwal/absensi/nilai/presensi screen tersambung API, antre Hive offline + badge + sync otomatis (connectivity) + tombol manual, unit test parsing (3 ✅, `flutter analyze` bersih).
- Backend lengkap: 17 modul (17 suites / 60 test ✅). Baru: `tahun-ajaran` (list/aktif/create/aktifkan), `guru` enroll/hapus wajah (consent + omit embedding di API), `absensi-guru` hariIni, presensi face/geo/fallback, verifikasi admin. `openapi.yaml` 34 paths unik.
- Mobile nilai bulk (repo+screen, filter mapel/semester/jenis, input 0-100, bulk 1 request), presensi (geo+fallback+face seam, enroll info di profil), `flutter analyze` bersih + `flutter test` 3/3 ✅.
- Push + kenaikan kelas: `notifikasi` (FCM modular v14, mode LOG tanpa kredensial, bersih-bersih token mati, hook broadcast pengumuman best-effort, log terkirim, 3 test), `POST /rombel/naik-kelas` (preview per rombel + eksekusi 1 transaksi + riwayat_kelas + audit, 3 test), migrasi `tambah_notifikasi` ✅. Total 18 suites / 66 test ✅.
